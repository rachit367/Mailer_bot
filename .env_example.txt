EMAIL_USER=
EMAIL_PASS=

DAILY_LIMIT=10
DELAY_MIN=20000
DELAY_MAX=30000

OPENROUTER_API_KEY=
OPENROUTER_MODEL=openrouter/hunter-alpha
OPENROUTER_FALLBACK_MODELS=stepfun/step-3.5-flash:free

# SMTP recipient probe — drops invalid addresses before sending.
# Set to "false" if you're on a network that blocks port 25 (most residential ISPs do).
SMTP_PROBE=true
# Optional: HELO domain used during the probe. Default: gmail.com
# SMTP_VERIFY_HELO=gmail.com
